## 왕혁준

### 1. 선수 이름만 있는 데이터의 한계

- **문제상황:** `world_cup_comparisons` 파일은 선수 이름만 컬럼으로 갖고 있어서 동명이인 문제가 발생함.
- **원인분석:** 이름만으로는 특정 선수를 식별할 수 없으니 동일 이름을 가진 다른 선수를 구분할 수 없음.
- **해결방법:** 선수 이름과 함께 출생연도 데이터를 포함한 다른 파일을 찾아서 추출함.
- **결과:** 이름 + 출생연도 기준으로 선수를 특정할 수 있게 되어 동명이인 문제를 해결할 기반을 마련함.



### 2. 불완전한 연도별 데이터

- **문제상황:** 1930~2014 월드컵 데이터 파일을 찾았지만, 1954 이전과 2014 데이터에 누락과 오류가 많았음.
- **원인분석:** 원본 데이터 자체의 결측과 기록 오류로 인해 특정 연도 데이터가 온전하지 않았음.
- **해결방법:** `nunique()` 등을 활용해 데이터가 온전히 들어있는 연도를 확인한 뒤, 1954~2010만 추출함. 이후 2014~2022 선수단 데이터는 팀원이 웹 크롤링을 통해 확보함.
- **결과:** 1954~2010은 안정적으로 확보, 2014~2022는 새롭게 크롤링한 데이터를 결합할 수 있었음.



### 3. 국가명 표기 불일치

- **문제상황:** 데이터가 온전히 들어있는 연도에도 나라 이름이 다른 방식으로 표기되어 있거나 특수한 사례(한국, 독일, 러시아)가 존재함.
- **원인분석:** 국가명이 역사적·정치적 상황(예: 동독·서독, 소련·러시아)이나 표기법 차이로 다르게 기록됨.
- **해결방법:** 여러 이름으로 쓰인 나라들을 리스트로 뽑아 현재 나라명으로 매핑함. 독일의 경우 1974년에 동독과 서독이 동시 출전했지만, 최종 목표가 본선 진출 횟수 평균 계산이므로 따로 처리하지 않았음.
- **결과:** 표준화된 국가명으로 통일하여 분석의 일관성을 확보함.



### 4. 선수 이름 표기 불일치와 정규화

- **문제상황:** 2014 이후 데이터를 합치는 과정에서 이름 표기법(순서, 하이픈, 특수문자, 별칭 등) 차이로 출전 횟수 카운팅이 제대로 작동하지 않음.
- **원인분석:** 국가별로 이름 순서(성/이름), 별칭 사용, full name vs first name 혼용 등 규칙이 제각각이었음. 특히 한국(2022), 일본(2022), 브라질(2018, 2022)의 경우가 카운팅 오류가 가장 많았음.
- **해결방법:**
    - `unicodedata`의 `NFKD`로 정규화 규칙을 통일.
    - 한국: `len(token)` 조건문으로 성/이름 순서를 조정.
    - 일본: 규칙 정하기 어려워 직접 일일이 수정.
    - 브라질: 별칭/혼용 문제 → 직접 데이터 파일 수정.
    - 최종적으로 선수 이름과 생년월일을 합쳐 `Player_ID` 생성.
- **결과:** `Player_ID` 기준으로 동일인/중복 여부를 체크할 수 있게 되었고, 이름 정규화 문제로 인한 출전 횟수 집계 오류를 최소화함.

---

## 안수이

- **문제 상황**
    
    FIFA 전체 선수 데이터와 월드컵 본선 선수 데이터를 선수 이름 기준으로 병합하려 했으나, 이름 불일치 문제 발생.
    
    예) sonheungmin vs heungminson → 단순 유니코드 정규화로는 해결되지 않음.
    
- **원인 분석**
    - 이름 표기 방식 차이: 데이터셋마다 이름 순서(성/이름) 다름
    - 대소문자, 공백 차이: 같은 선수라도 표기 규칙이 일관되지 않음
    - 정규화 한계: 유니코드 정규화만으로는 편집 순서까지 해결 불가
- **해결 방법**
    1. 전처리: 모든 이름을 소문자 변환 → 공백 제거 처리
    2. 보조 키 활용: 같은 출전 년도와 생년월일을 가진 선수끼리만 후보군으로 제한
    3. 유사도 매칭 적용: **`lev_distance`** 라이브러리 (편집거리 알고리즘)을 사용하여 후보군 중 편집거리가 가장 작은 이름을 최종 매칭으로 선택
- **결과**
    
    모든 매칭되지 않던 데이터를 완전하게 매칭할 수 있었다.→ 데이터 병합 성공.
    

---

## 신지섭

- **문제 상황**
    
    각 Dataset 별로 나라 이름이 다른 형태로 되어있어서 매핑과정이 필요했음
    
- **원인 분석**
    
    Dataset별로 다른 맵이 필요했고 또 두 나라가 같은 영문명을 공유해야하는 경우가 있어서 2개의 키가 하나에 매핑되는 경우가 있었음
    
- **해결 방법**
    
    Dataset의 병합과정을 단계별 파일로 구분하고 병합후 결측치나 매핑이 안된 요소를 확인하여 매핑을 수정 두 나라가 같은 경우에는 csv파일을 직접 수정
    

---

## 김진

### 1.

- **문제 상황**
    - 평균 랭킹 데이터프레임(`avg_ranking_df`)과 평균 포인트 데이터프레임(`avg_points_df`)을 하나로 합쳐 새로운 파일을 만들고자 했으나, 의도한 대로 데이터가 정리되지 않음
- **원인 분석**
    - 두 데이터프레임을 `year` 컬럼만으로 join하면서, 동일한 `year`에 여러 팀/이름이 존재할 경우 행이 중복되어 예상치 못한 데이터 확장이 발생
- **해결 방법**
    - join 시 `name`과 `year`를 함께 key 컬럼으로 사용하여 팀별·연도별로 정확하게 매칭되도록 수정

### 2.

- **문제 상황**: 히트맵 분석 중 가독성 문제 발생
- **원인 분석**: 변수 수가 많아 색상 구분 및 annot 겹침
- **해결 방법**:
    - 데이터를 조별리그, 16강 이상, 4강 이상 등 단계별로 나눠 별도 히트맵 생성
    - 필요시 상위 변수만 선택하여 다시 시각화
